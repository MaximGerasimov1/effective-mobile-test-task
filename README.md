# Python App with Nginx Reverse Proxy

Тестовое задание для **Effective Mobile**. Проект представляет собой контейнеризированное Python-приложение, работающее за прокси-сервером Nginx.

## 🛠 Технологии
* **Python 3.11-slim** (Base image для бэкенда)
* **Nginx:alpine** (Reverse Proxy)
* **Docker & Docker Compose** (Оркестрация)
* **Environment Variables** (Конфигурация через `.env`)

---

## 🏗 Архитектура

Схема взаимодействия компонентов:

```ascii
      User (Browser/Curl)
             |
             | (Port 80)
             ▼
    +-----------------------+
    |     Nginx Container   |  <-- Reverse Proxy
    |    (nginx-service)    |
    +-----------------------+
             |
             | (Internal Network)
             ▼
    +-----------------------+
    |   Python Backend      |  <-- App Server (Port 8080)
    |  (backend-service)    |
    +-----------------------+
```

1. Nginx принимает внешние HTTP-запросы на порту 80 (или указанном в .env).

2. Согласно конфигурации nginx.conf, он перенаправляет трафик на сервис backend во внутреннюю сеть Docker.

3. Python бэкенд обрабатывает запрос и возвращает ответ "Hello from Effective Mobile!".

## 🚀 Запуск проекта
### 1. Подготовка окружения
Создайте файл .env в корневой директории. Вы можете скопировать его из примера:<br>

```cp .env.example .env```


### 2. Сборка и запуск
Выполните команду для сборки образов и запуска контейнеров:<br>

```docker-compose up --build```

(Добавьте флаг -d, если хотите запустить в фоновом режиме)

## ✅ Проверка работоспособности
После запуска проект будет доступен по адресу http://localhost.

### Вариант 1: Через терминал (Curl)

```curl http://localhost```

**Ожидаемый ответ:** Hello from Effective Mobile!

### Вариант 2: Через браузер

Перейдите по ссылке: http://localhost

## 🧹 Остановка и удаление
Чтобы остановить проект и удалить контейнеры, используйте:

```docker-compose down```

## 🛡 Особенности реализации (Best Practices)
* **Безопасность:** В Dockerfile бэкенда создан не-привилегированный пользователь backenduser. Приложение не работает от root.

* **Гибкость:** Все порты и имена сервисов вынесены в переменные окружения (.env).

* **Оптимизация:** Используются легковесные образы (slim, alpine) и зафиксированы версии базовых имиджей для предотвращения проблем с latest.

* **Чистота:** Использование .gitignore для исключения лишних файлов из сборок и репозитория.
